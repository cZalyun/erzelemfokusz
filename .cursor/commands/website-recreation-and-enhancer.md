# website-recreation-and-enhancer

Do the recreation of the provided website end to end down to the last detail with modern 2026 technologies and latest best practices in a modularised manner. using industry standards.

enhance it with fancy css and modern UI and UX practices 


add multi language support english and hungarian  also light and dark mode using system default

use modern looking fancy ways to create the ui it should be easily extendable configurable. it should be easily configurable to enable disable features and parts of the site like languages pages etc. and every configuration sversion hould be up to standards logically visually and code quality wise.

it should be a pure frontend application using best practices for SEO too.

it should be deployed to github pages under the provided custom domain automatically always from the main branch
in case of not existing path auto redirect to the / path should be added and also every SEO related function like open graph images and similar things. the domain the gh pages will be used is the provided domain. 

use clean code best practices like early returns modularisation. high configurability level. the code should be striclty typed too.


all assets should be added too (images, videos, logos etc..) properly from the original site also in a configurable manner. 

the final site must be optimized to mobile too it should look stunning in desktop and on mobile too using best practices for mobile and desktop too.

always verify and use the latest versions form each technologies. You must use the latest and greatest all the time. during implementation consult with the corresponding proper official docs as well.

the original website should be scraped end to end every page every part every text all images and everything there is should be ported